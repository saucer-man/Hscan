#!/usr/bin/env python3

from libs.core.datatype import AttribDict
from libs.core.log import LOGGER

# scan paths
paths = AttribDict()

# cmdLineOptions
cmdLineOptions = AttribDict()

# conf target
conf = AttribDict()

# logger
logger = LOGGER(logger="Hscan")
